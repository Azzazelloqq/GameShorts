# 📱 Safe Area - Руководство по настройке в Unity Editor

## 🎯 Что делает Safe Area?

Safe Area автоматически адаптирует ваш UI под вырезы экрана (notch), камеры и скругленные углы на мобильных устройствах.

![Safe Area Example](https://i.imgur.com/example.png)

## 🚀 Быстрый старт

### Шаг 1: Создайте Canvas
1. **GameObject → UI → Canvas**
2. Настройте Canvas Scaler по необходимости

### Шаг 2: Добавьте SafeAreaCanvas
1. Выберите Canvas
2. **Add Component → Safe Area Canvas**
3. Включите **Auto Refresh Children** ✅

### Шаг 3: UI будет создан автоматически
При добавлении SafeAreaCanvas автоматически создастся панель SafeAreaPanel с компонентом SafeAreaFitter.

---

## ⚙️ Как работают Anchors в SafeAreaFitter

### 🎨 В Unity Editor вы увидите:

```
Inspector SafeAreaFitter
┌─────────────────────────────┐
│ ✅ Safe Area Fitter         │
├─────────────────────────────┤
│ Настройки Safe Area         │
│ ┌───────────────────────┐   │
│ │    [✅] Верх          │   │
│ │ ┌─────────────────┐   │   │
│ │ │                 │   │   │
│ │ │   Safe Area     │   │   │
│ │ │                 │   │   │
│ │ └─────────────────┘   │   │
│ │    [✅] Низ           │   │
│ └───────────────────────┘   │
│ [✅]Лево        [✅]Право    │
└─────────────────────────────┘
```

### 🔧 Что происходит под капотом:

1. **БЕЗ SafeAreaFitter:**
   - RectTransform anchors: `Min(0,0)` `Max(1,1)` - занимает весь экран
   - UI может попасть под вырез

2. **С SafeAreaFitter (все стороны включены):**
   - Anchors автоматически изменяются на safe area
   - Например: `Min(0.05, 0.03)` `Max(0.95, 0.92)`
   - UI остается в безопасной зоне

---

## 📋 Пошаговая настройка

### Вариант 1: Полная защита (рекомендуется для основного контента)

1. Создайте пустой GameObject под Canvas
2. Добавьте **SafeAreaFitter**
3. В Inspector увидите визуальную настройку:
   - ✅ Все 4 галочки включены (Верх, Низ, Лево, Право)
4. Поместите весь UI внутрь этого объекта

### Вариант 2: Только верхняя панель (для header/статус бара)

1. Создайте панель для header
2. Установите RectTransform:
   - Anchor Preset: **Top Stretch**
   - Height: нужная высота (например, 100)
3. Добавьте **SafeAreaFitter**
4. В Inspector:
   - ✅ Верх
   - ❌ Низ, Лево, Право

### Вариант 3: Только нижняя панель (для навигации)

1. Создайте панель для навигации
2. Установите RectTransform:
   - Anchor Preset: **Bottom Stretch**
   - Height: нужная высота (например, 80)
3. Добавьте **SafeAreaFitter**
4. В Inspector:
   - ❌ Верх, Лево, Право
   - ✅ Низ

---

## 🎮 Использование пресетов

В Inspector SafeAreaFitter есть кнопки быстрых пресетов:

| Кнопка | Что делает | Когда использовать |
|--------|------------|-------------------|
| 📱 **Полный экран** | Включает все стороны | Основной контент |
| 🔝 **Только верх** | Только верхняя safe area | Header, статус бар |
| 🔽 **Только низ** | Только нижняя safe area | Навигация, кнопки |
| ↔️ **Горизонталь** | Левая и правая стороны | Боковые панели |
| ↕️ **Вертикаль** | Верх и низ | Контент по центру |
| ❌ **Отключить** | Выключает safe area | Фоновые элементы |

---

## 🖼️ Примеры структуры в Hierarchy

### Простая структура:
```
Canvas
├── SafeAreaPanel (SafeAreaFitter - все стороны)
│   ├── Header
│   ├── Content
│   └── Footer
```

### Продвинутая структура:
```
Canvas (SafeAreaCanvas)
├── Background (без SafeAreaFitter - на весь экран)
├── HeaderPanel (SafeAreaFitter - только верх)
│   └── Title, Menu Button
├── MainContent (SafeAreaFitter - все стороны)
│   └── ScrollView, Buttons
└── BottomNav (SafeAreaFitter - только низ)
    └── Home, Settings, Profile
```

---

## 🧪 Тестирование в Editor

### Включение симуляции Notch:
1. В **Game View** выберите соотношение сторон:
   - **Free Aspect** или создайте свое (например, 1125x2436 для iPhone X)
2. Запустите Play Mode
3. SafeAreaFitter автоматически симулирует notch

### Проверка работы:
1. Включите **Show Debug Info** в SafeAreaFitter
2. Откройте Console - увидите информацию о safe area
3. Меняйте размер Game View - UI должен адаптироваться

---

## 💡 Советы

### ✅ Делайте:
- Используйте SafeAreaCanvas на корневом Canvas для централизованного управления
- Создавайте отдельные панели для разных зон экрана
- Тестируйте в разных ориентациях (Portrait/Landscape)
- Используйте Additional Padding для дополнительных отступов

### ❌ Не делайте:
- Не ставьте SafeAreaFitter на сам Canvas (ставьте на child объект)
- Не используйте для элементов с абсолютным позиционированием
- Не забывайте про Landscape ориентацию

---

## 🔍 Отладка

### Если UI не адаптируется:
1. Проверьте, что SafeAreaFitter добавлен на объект с RectTransform
2. Убедитесь, что нужные стороны включены в Inspector
3. Нажмите "Force Refresh" в Inspector
4. Проверьте Anchor Presets у RectTransform

### Визуальная отладка:
1. В Scene View при выбранном объекте с SafeAreaFitter видны зеленые границы safe area
2. В Inspector видна визуализация настроек
3. Кнопка "📊 Информация" в SafeAreaCanvas показывает детальную информацию

---

## 📝 Код для программного создания:

```csharp
// Создать Canvas с SafeArea
GameObject canvasGO = new GameObject("UI");
Canvas canvas = canvasGO.AddComponent<Canvas>();
canvas.renderMode = RenderMode.ScreenSpaceOverlay;

// Добавить SafeAreaCanvas для управления
SafeAreaCanvas safeCanvas = canvasGO.AddComponent<SafeAreaCanvas>();

// Создать панель с полной safe area
GameObject mainPanel = new GameObject("MainPanel");
mainPanel.transform.SetParent(canvas.transform, false);

RectTransform rect = mainPanel.AddComponent<RectTransform>();
rect.anchorMin = Vector2.zero;  // (0,0)
rect.anchorMax = Vector2.one;   // (1,1)
rect.offsetMin = Vector2.zero;
rect.offsetMax = Vector2.zero;

// Добавить SafeAreaFitter - он автоматически настроит anchors
SafeAreaFitter fitter = mainPanel.AddComponent<SafeAreaFitter>();
// По умолчанию все стороны включены

// Теперь добавляйте UI элементы внутрь mainPanel
// Они будут в безопасной зоне!
```

---

## 🎯 Результат

После настройки ваш UI будет:
- ✅ Автоматически избегать вырезов камеры
- ✅ Не попадать под скругленные углы
- ✅ Адаптироваться при повороте экрана
- ✅ Работать на всех устройствах (iOS и Android)

