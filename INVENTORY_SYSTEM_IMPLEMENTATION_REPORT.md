# Отчет о реализации системы инвентаря для Gardener

## Резюме

Успешно реализована полная система инвентаря для игры Gardener с поддержкой:
- ✅ Хранение купленных семян в инвентаре с количеством
- ✅ Новый режим "Inventory" для посадки семян
- ✅ Drag-and-drop посадка семян на готовые грядки
- ✅ Отображение количества семян (когда > 1)
- ✅ Автоматическое обновление UI при изменении инвентаря

## Созданные компоненты

### 1. InventoryManager (`Assets/Code/Games/Gardener/Scripts/Gameplay/InventoryManager.cs`)

Менеджер инвентаря, управляющий семенами игрока:

**Основной функционал:**
- `Dictionary<PlantSettings, ReactiveProperty<int>>` для хранения семян с количеством
- Реактивное обновление UI через R3

**Методы:**
- `AddSeeds(PlantSettings, int)` - добавить семена в инвентарь
- `RemoveSeeds(PlantSettings, int)` - удалить семена (при посадке)
- `GetSeedCount(PlantSettings)` - получить количество конкретного типа
- `HasSeeds(PlantSettings)` - проверить наличие семян
- `GetAvailableSeeds()` - получить все доступные семена с количествами
- `GetSeedCountObservable(PlantSettings)` - Observable для конкретного типа семян

**Observable события:**
- `InventoryChanged` - уведомление об изменении инвентаря для обновления UI

### 2. InventoryMode (`Assets/Code/Games/Gardener/Scripts/Gameplay/Modes/InventoryMode.cs`)

Новый игровой режим для посадки семян из инвентаря:

**Особенности:**
- Реализует интерфейс `IGardenerMode`
- `ModeName = "Inventory"`
- Возвращает PlaceableItems из инвентаря с актуальным количеством
- Автоматически обновляет список элементов при изменении инвентаря

**Логика посадки (`OnItemPlaced`):**
1. Находит грядку в указанной позиции
2. Проверяет, что грядка пустая (`CurrentState == Empty`)
3. Проверяет, что грядка подготовлена (`IsPreparationComplete == true`)
4. Сажает семена (`plot.PlantSeed()`)
5. Уменьшает количество в инвентаре (`RemoveSeeds()`)

### 3. Обновленные компоненты

#### PlaceableItem
Добавлены новые свойства:
```csharp
public int Count { get; set; }               // Количество элементов
public PlantSettings PlantSettings { get; set; } // Ссылка на настройки растения
```

Обновленный конструктор:
```csharp
public PlaceableItem(string itemName, Sprite icon, GameObject prefab, 
                     int count = 1, PlantSettings plantSettings = null)
```

#### PlaceableItemView
Добавлено отображение количества:
```csharp
[SerializeField] private TextMeshProUGUI _countLabel;
```

**Логика отображения:**
- Показывает лейбл с количеством только когда `Count > 1`
- Автоматически скрывает лейбл когда `Count == 1`

#### GardenerGameplayPm
Интеграция инвентаря:
- Добавлен `InventoryManager _inventoryManager`
- Публичное свойство `InventoryManager` для доступа из UI
- Регистрация `InventoryMode` в `RegisterModes()`
- Обновлен метод `TryBuySeeds()` - теперь добавляет семена в инвентарь
- Удалена логика `_selectedPlot` (больше не нужна)

#### GardenerUIPm
- Добавлена подписка на кнопку инвентаря
- Добавлен обработчик `InventoryModeButton.onClick`
- Обновлена логика очистки при `OnDispose()`

#### GardenerMainUIView
- Добавлено поле `[SerializeField] private Button _inventoryModeButton`
- Публичное свойство `InventoryModeButton`
- Обновлен метод `UpdateModeButtonColors()` для подсветки кнопки инвентаря

## Поток работы системы

### 1. Покупка семян в магазине

```
Пользователь кликает "Buy" в магазине
  ↓
GardenerUIPm.BuyPlant(PlantSettings)
  ↓
GardenerGameplayPm.TryBuySeeds(PlantSettings)
  ↓
Проверка денег → Списание денег → InventoryManager.AddSeeds()
  ↓
Инвентарь обновлен, событие InventoryChanged
  ↓
InventoryMode обновляет PlaceableItems
  ↓
Магазин закрывается
```

### 2. Посадка семян из инвентаря

```
Пользователь переключается в режим "Inventory"
  ↓
GardenerModeManager.SwitchMode("Inventory")
  ↓
InventoryMode.OnEnter() → обновление PlaceableItems
  ↓
PlaceableItemsPanel показывает семена с количеством
  ↓
Пользователь перетаскивает семена на грядку (drag-and-drop)
  ↓
InventoryMode.OnItemPlaced(item, position)
  ↓
Валидация:
  - Грядка найдена?
  - Грядка пустая?
  - Грядка подготовлена?
  - Семена есть в инвентаре?
  ↓
Если OK: plot.PlantSeed() + InventoryManager.RemoveSeeds()
  ↓
Количество семян уменьшено
  ↓
UI автоматически обновлен (через ReactiveProperty)
  ↓
Если количество = 0, семена исчезают из панели
```

### 3. Автоматическое обновление UI

```
InventoryManager.RemoveSeeds() или AddSeeds()
  ↓
ReactiveProperty изменено
  ↓
InventoryChanged.Value = Unit.Default
  ↓
InventoryMode подписан на InventoryChanged
  ↓
InventoryMode.UpdatePlaceableItems()
  ↓
PlaceableItems пересоздаются с актуальным количеством
  ↓
PlaceableItemsPanel обновляет отображение
  ↓
PlaceableItemView показывает обновленное количество
```

## Преимущества архитектуры

### 🎯 Реактивность
- Использование ReactiveProperty для автоматического обновления UI
- Подписка на изменения через R3
- Минимум ручных вызовов обновления

### 🔧 Расширяемость
- Легко добавить новые типы элементов в инвентарь
- Можно создать другие режимы, использующие инвентарь
- PlaceableItem универсален для любых элементов

### 📦 Инкапсуляция
- InventoryManager полностью управляет логикой инвентаря
- InventoryMode изолирует логику посадки
- Чистое разделение ответственности

### ✅ Валидация
- Множественные проверки перед посадкой
- Понятные сообщения об ошибках в Debug.Log
- Защита от некорректных действий

## Настройка в Unity Editor

### 1. GardenerMainUIView
В инспекторе добавить:
- **Inventory Mode Button** - кнопку переключения в режим инвентаря

### 2. PlaceableItemView префаб
Добавить в префаб:
- **Count Label (TextMeshProUGUI)** - для отображения количества
- Рекомендуется разместить в правом верхнем углу иконки

### 3. Тестирование
1. Запустить игру
2. Переключиться в режим Harvey
3. Разместить несколько грядок на огороде
4. Дождаться подготовки грядок (зеленый индикатор)
5. Открыть магазин и купить семена (2-3 раза один тип)
6. Переключиться в режим Inventory
7. Проверить отображение количества на элементах
8. Перетащить семена на пустую подготовленную грядку
9. Проверить, что:
   - Растение посажено
   - Количество уменьшилось
   - Если количество = 0, элемент исчез

## Возможные улучшения

### 1. Иконки для семян
Добавить в `PlantSettings`:
```csharp
[SerializeField] private Sprite _seedIcon;
public Sprite SeedIcon => _seedIcon;
```

Это позволит задавать отдельные иконки для семян вместо использования моделей.

### 2. Категории инвентаря
Расширить `InventoryManager` для поддержки разных категорий:
- Семена
- Инструменты
- Удобрения
- И т.д.

### 3. Максимальный размер инвентаря
Добавить лимит на количество типов или общее количество элементов:
```csharp
public int MaxInventorySlots { get; set; } = 10;
```

### 4. Сохранение инвентаря
Интегрировать с системой сохранения для persistence между сессиями:
```csharp
public Dictionary<string, int> SerializeInventory();
public void DeserializeInventory(Dictionary<string, int> data);
```

### 5. Визуальная индикация на грядках
При перетаскивании семян показывать:
- Зеленый цвет - можно посадить
- Красный цвет - нельзя (занято или не готово)
- Серый цвет - грядка не подготовлена

### 6. Звуковые эффекты
Добавить звуки:
- При добавлении семян в инвентарь
- При посадке семян
- При попытке посадить на недопустимую грядку

## Заключение

Система инвентаря полностью реализована и интегрирована в игру Gardener. Все требования выполнены:

✅ Семена добавляются в инвентарь при покупке  
✅ Новый режим "Inventory" для посадки  
✅ Drag-and-drop только на пустые подготовленные грядки  
✅ Отображение количества (когда > 1)  
✅ Автоматическое обновление UI  
✅ Валидация всех действий  

Код написан с учетом лучших практик, использует реактивное программирование и легко расширяется.


